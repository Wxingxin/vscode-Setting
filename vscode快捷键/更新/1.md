# ``

```json
"`${1}`": {
  "prefix": "qq",
  "body": [
    "`${1}`"
  ],
  "description": "`${1}`"
}
```

# dotenv

## db

```json
"dotenv mongodb": {
  "prefix": "dotenvmongodb",
  "body": [
    "# mongodb",
    "# TODO:  npm i mongodb",
    "MONGODB_URI=mongodb://127.0.0.1:27017",
    " #使用的数据库名称，如果数据库不存在，MongoDB 会自动创建",
    "MONGODB_DB=${1:😎😎😎}"
  ],
  "description": "dotenv mongodb"
},
```

## bcrypt

```json
"dotenv bcrypt": {
  "prefix": "dotenvbcrypt",
  "body": [
    "# bcrpyt",
    "# TODO: npm i bcrypt  TODO: npm i @types/bcrypt",
    "SALT_ROUNDS=10 # 加密强度",
    "",
    ""
  ],
  "description": "dotenv bcrypt"
}
```

## jose

```json
"dotenv jose": {
  "prefix": "dotenvjose",
  "body": [
    "# jose",
    "# TODO: npm i jose",
    "SECRET_KEY=10 #服务器用于加密和验证 Token 的密钥",
    "",
    ""
  ],
  "description": "dotenv jose"
}
```

## email

```json
"dotenv email": {
  "prefix": "dotenvemail",
  "body": [
    "# email SMTP(QQ)",
    "# TODO: npm i nodemailer TODO: npm i @types/nodemailer",
    "SMTP_HOST=smtp.qq.com #SMTP 服务器地址",
    "SMTP_PORT=465 #465 = SSL加密",
    "SMTP_SECURE=true #true 表示使用 SSL",
    "SMTP_USER=${1:3059950762@qq.com}#发送信息的邮件",
    "SMTP_PASS=${2:😎😎😎}  #邮件授权码",
    "MAIL_FORM_NAME=${3:weiandgao}  #用户收到邮件时显示的发件人名字",
    "",
    ""
  ],
  "description": "dotenv email"
}
```

## minio

```json
"dotenv minio": {
  "prefix": "dotenvminio",
  "body": [
    "# minio",
    "# TODO:  npm i minio TODO: .\\minio.exe server data --console-address \":9001\"",
    "MINIO_ENDPOINT=127.0.0.1 # MinlO 服务地址 (如 127.0.0.1)",
    "MINIO_PORT=9000 # 默认端口",
    "MINIO_USE_SSL=false # 是否使用 https",
    "MINIO_ACCESS_KEY=minioadmin # 账号",
    "MINIO_SECRET_KEY=minioadmin # 密码",
    "MINIO_BUCKET=${1:😎😎😎} # 存储桶名称",
    "MINIO_PUBLIC_ENDPOINT=127.0.0.1:9000 # 公开访问地址",
    "",
    ""
  ],
  "description": "dotenv minio"
}
```

# nextjs lib

```json
"nextjs lib link add": {
  "prefix": "nexlinkadd",
  "body": [
    "// TODO: XxxCollectionDoc * 3",
    "export type XxxCollectionDoc = {",
    "  _id?: ObjectId;",
    "",
    "};",
    "// 返回强类型的消息集合，供 API 路由直接执行 insert/find 等操作。",
    "// TODO: getXxxCollection * 1",
    "export async function getXxxCollection(): Promise<",
    "  Collection<XxxCollectionDoc>",
    "> {",
    "  const db = await getMongoDb();",
    "",
    "  //TODO: MONGODB_COLLECTION_XXX * 1 || xxx * 1",
    "  const collectionName = process.env.MONGODB_COLLECTION_XXX || \"xxx\";",
    "",
    "  if (!collectionName) {",
    "    throw new Error(`Missing collectionName`);",
    "  }",
    "",
    "  return db.collection<XxxCollectionDoc>(collectionName);",
    "}",
    ""
  ],
  "description": "nextjs lib link add"
},
```

## 总结
