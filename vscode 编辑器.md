下面**直接给你可操作的方法**，把 **VS Code「代码编辑区域」背景颜色**改掉。按**推荐程度**从上到下。

---

## ✅ 方法一（最推荐）：修改 `settings.json`

这是**最稳、最可控**的方式。

### 1️⃣ 打开设置文件

* `Ctrl + Shift + P`
* 输入：`Preferences: Open Settings (JSON)`

### 2️⃣ 添加 / 修改配置

```json
{
  "workbench.colorCustomizations": {
    "editor.background": "#1e1e2e"
  }
}
```

📌 说明：

* `editor.background` 👉 **代码编辑区背景**
* 支持 `#RGB / #RRGGBB`
* 立刻生效，无需重启

---

## 🎯 常用可一起改的编辑区颜色（强烈建议）

```json
{
  "workbench.colorCustomizations": {
    "editor.background": "#1e1e2e",
    "editor.foreground": "#cdd6f4",
    "editorLineNumber.foreground": "#6c7086",
    "editorLineNumber.activeForeground": "#ffffff",
    "editorCursor.foreground": "#f5e0dc",
    "editor.selectionBackground": "#45475a",
    "editor.lineHighlightBackground": "#313244"
  }
}
```

### 对应关系速查表

| 配置项                                 | 作用    |
| ----------------------------------- | ----- |
| `editor.background`                 | 编辑区背景 |
| `editor.foreground`                 | 代码文字  |
| `editorLineNumber.foreground`       | 行号    |
| `editorLineNumber.activeForeground` | 当前行号  |
| `editor.lineHighlightBackground`    | 当前行背景 |
| `editor.selectionBackground`        | 选中文本  |

---

## ❌ 方法二（不推荐）：改主题文件

如果你去改：

```json
dark-plus.json
```

❌ 缺点：

* VS Code 更新会被覆盖
* 不方便调试
* 不适合个人定制

✅ **只有在你开发主题插件时才用**

---

## 🧠 方法三：只对某个主题生效（高级）

```json
{
  "[Default Dark+]": {
    "workbench.colorCustomizations": {
      "editor.background": "#0f172a"
    }
  }
}
```

📌 好处：

* 不影响其他主题
* 非常适合你现在做 VS Code 主题的场景

---

## 🧪 如果你发现「改了没效果」

按顺序排查：

1. 有没有拼错

   ```json
   editor.background ❌ editorBackground
   ```
2. 是否被主题覆盖
3. 是否在 **用户设置** 而不是 **工作区设置**
4. 是否在 `[]` 主题作用域内

---

## 🔥 推荐几组好看的背景色（直接用）

| 风格           | 颜色        |
| ------------ | --------- |
| VS Code 原生暗色 | `#1e1e1e` |
| GitHub Dark  | `#0d1117` |
| 深蓝护眼         | `#0f172a` |
| Catppuccin   | `#1e1e2e` |
| 极暗黑          | `#09090b` |

---

## 🧩 如果你下一步想做这些，我可以直接给你配置

* ✔ 当前行高亮更明显
* ✔ 编辑区和侧边栏不同色
* ✔ 光标 + 选中颜色统一风格
* ✔ 做成 **你自己的 VS Code 主题插件**
* ✔ 专门适合 React / TS / NestJS 的配色

你只要说一句：
👉 **“我想整体调一套主题”** 或 **“我要做主题插件”**
