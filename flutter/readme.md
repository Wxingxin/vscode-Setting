# 我看见别人在vscode中写flutter代码有嵌套的虚线，是什么插件吗

你说的在 Visual Studio Code 中写 Flutter 代码时，看到 “嵌套的小虚线”——基本上 **不是一个独立的插件**，而是 VS Code 与 Dart/Flutter 扩展提供的一项「UI 辅助线」功能。具体如下：

---

### ✅ 是什么

* 一位回答指出：这不是扩展，而是 VS Code 的内置设置。([Stack Overflow][1])
* 在 Dart/Flutter 的设置里，有选项名为 “Flutter UI Guides” 或类似，启用后即可看到“破折线风格”的层级／嵌套指示线。([Medium][2])
* 也有用户提到使用了插件如 Guides 来实现缩进辅助线（包括虚线样式）：“I found one extension named Guides. You can change these indent lines to any style in settings for that extension.” ([Reddit][3])

---

### 🛠 如何开启／配置

1. 在 VS Code 中打开 **设置**（例如按 `Ctrl+,` 或 `⌘+,`）
2. 搜索关键词：`flutter ui guides` 或 `ui guides flutter`
3. 在设置里找到类似于 “Dart: Preview Flutter UI Guides” 或 “Flutter UI Guides” 的选项，勾选它。([Medium][2])
4. 重启 VS Code 后，在你的 Flutter widget 层级中就能看到左侧或缩进区域有虚线，帮助你快速识别嵌套结构。
5. 如果你想更灵活／定制样式，还可以安装 “Guides” 插件，然后在其设置里配置虚线、颜色、缩进线样式等。([Reddit][3])

---

### 🎯 对你这种情况的建议（你正在用 Vue/Angular/前端）

虽然你当前主要学习的是前端 (Vue、Angular) 但如果你将来也要用 Flutter 或想在 VS Code 中增强可读性，建议你也开启这个功能：

* 即使不是 Flutter 项目，插件 “Guides” 对 HTML／JS／TS／Vue 等嵌套结构也有帮助。
* 你专注前端时，也可以将类似功能用于 Vue 的 template 嵌套、HTML 结构中。
* 考虑配合 “Indent‑Rainbow” 插件突出缩进层次，或者 “Blockman” 显示块结构辅助线。

  > “Indent Rainbow … great to visualize blocks of code.” ([Reddit][3])

---

如果你愿意，我可以 **帮你**找到几个 VS Code 插件列表，并针对前端（Vue/Angular）配置“缩进／嵌套虚线”样式，方便你在写 HTML／JSX／TSX 时也有类似视觉辅助。要不要？



# 我的vscode中原本就有对齐的虚线，他们不重和，我想去掉原本的虚线
明白了 ✅
你现在的情况是：
VS Code 里本身有一组 **“对齐缩进虚线”**（默认的缩进参考线），而 Flutter/Dart 扩展又添加了 **“Flutter UI Guides”** 虚线，两组线 **不重合**，导致界面里出现“多层虚线”，看起来很乱。

你想**去掉 VS Code 自带的那组虚线**，只保留 Flutter 的那组。
下面是详细解决办法 👇

---

## 🧭 方法一：关闭 VS Code 默认缩进参考线

这是最干净的做法。

### ✅ 操作步骤

1. 打开设置：

   * 按 `Ctrl + ,`（或 mac 上 `⌘ + ,`）
2. 在搜索框输入：`indent guides`
3. 找到并关闭以下选项：

   ```
   Editor › Guides: Indentation
   ```

   把勾去掉。

   或者直接在 **settings.json** 中加入：

   ```json
   "editor.guides.indentation": false
   ```

这样，默认的缩进虚线就会消失，只剩下 Flutter UI Guides（那组彩色或虚线对齐的辅助线）。

---

## 🧭 方法二：改成更浅的颜色（如果你不想完全去掉）

如果你希望仍能稍微看到 VS Code 的缩进线，可以在 `settings.json` 中调淡颜色：

```json
"workbench.colorCustomizations": {
  "editorIndentGuide.background": "#00000000",
  "editorIndentGuide.activeBackground": "#00000000"
}
```

> `#00000000` 表示完全透明。你也可以改成浅灰，比如 `#4444`。

---

## 🧭 方法三：彻底禁用“Indent Rainbow”或“Guides”等插件

如果你之前装了类似的插件（会额外渲染缩进虚线），可以：

1. 打开命令面板：`Ctrl + Shift + P`
2. 输入 “Extensions: Show Installed Extensions”
3. 搜索：

   * **Indent-Rainbow**
   * **Guides**
4. 点击 ⚙️ → “禁用（Disable）”

---

## 🔍 总结

| 目标            | 设置键                               | 建议值     |
| ------------- | --------------------------------- | ------- |
| 去掉默认缩进虚线      | `"editor.guides.indentation"`     | `false` |
| 保留 Flutter 虚线 | `"dart.previewFlutterUiGuides"`   | `true`  |
| 改颜色（可选）       | `"workbench.colorCustomizations"` | 设置透明或浅灰 |

---

要我帮你写一个完整可复制的 `settings.json` 示例（包括美化 Flutter 的虚线样式）吗？
